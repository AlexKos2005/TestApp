<Window x:Class="TestApp.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestApp"
        mc:Ignorable="d"
        Title="MainWindowView" Height="450" Width="800">
    <Grid>
        <Canvas>
            <TextBlock Name="text1" Text="{Binding Count}" Width="100" Height="100" Canvas.Left="299" Canvas.Top="101"></TextBlock>
            <Button Name="bt1" Command="{Binding Click}" Width="100" Height="100" Canvas.Left="81" Canvas.Top="199"></Button>
        </Canvas>
    </Grid>
</Window>
